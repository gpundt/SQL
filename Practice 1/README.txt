The "Pundt_PE11" file is an SQL script. Creates a database with multiple entities containing Primary and Foreign keys.

"ER_Diagram.png" is what the final database should look like.

"SQL_script_execution.txt" is a TEE file showing functionality of script. Shows each command being executed
and describes each table that is created.
